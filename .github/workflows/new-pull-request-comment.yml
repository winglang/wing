name: PR Creation Comment

on:
  pull_request_target:
    types:
      - opened

permissions:
  contents: read
  pull-requests: write

jobs:
  comment:
    runs-on: ubuntu-latest
    if: ${{ !startsWith(github.head_ref, 'mergify/merge-queue/') }}
    steps:
      - name: Comment on new PR
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: |
            Thanks for opening this pull request! :tada:
            Please consult the [contributing guidelines](https://www.winglang.io/contributing) for details on how to contribute to this project.
            If you need any assistance, don't hesitate to ping the relevant owner over [Discord](https://t.winglang.io/discord).

            | Topic                                               | Owner                                                         |
            |-----------------------------------------------------|---------------------------------------------------------------|
            | Wing SDK and utility APIs                           | `@chriscbr`
            | Wing Console                                        | `@skyrpex`
            | JSON, structs, primitives and collections           | `@chriscbr`
            | Platforms and plugins                               | `@chriscbr`
            | Frontend resources (website, react, etc)            | `@chriscbr`
            | Language design                                     | `@chriscbr`
            | VSCode extension and language server                | `@chriscbr`
            | Compiler architecture, inflights, lifting           | `@chriscbr`
            | Wing Testing Framework                              | `@chriscbr`
            | Wing CLI                                            | `@chriscbr`
            | Build system, dev environment, releases             | `@chriscbr`
            | Library Ecosystem                                   | `@chriscbr`
            | Documentation                                       | `@boyney123`
            | SDK test suite                                      | `@chriscbr` 
            | Examples                                            | `@boyney123`
            | Wing Playground                                     | `@skyrpex`

          comment_tag: New PR
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
