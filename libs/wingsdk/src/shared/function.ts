/**
 * The default memory size for cloud functions, in MB.
 */
export const DEFAULT_MEMORY_SIZE = 1024;

/**
 * The default memory size for cloud functions, in MB.
 */
export const DEFAULT_CONCURRENCY = 10;
