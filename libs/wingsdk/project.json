{
  "name": "wingsdk",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "inputs": ["src"],
      "outputs": ["dist"],
      "dependsOn": ["^build"],
      "options": {
        "cwd": "apps/wingsdk",
        "script": "build"
      }
    }
  }
}