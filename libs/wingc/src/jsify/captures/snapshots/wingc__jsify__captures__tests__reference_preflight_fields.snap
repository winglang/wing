---
source: libs/wingc/src/jsify/captures/tests.rs
expression: "capture_ok(r#\"\n    bring cloud;\n    \n\t\tclass MyType {\n      s: str;\n      b: cloud.Bucket;\n\n      init() { \n        this.s = \"hello\"; \n        this.b = new cloud.Bucket();\n      }\n\n      inflight boom() {\n        assert(this.s.length > 0);\n        assert(this.b.list().length > 0);\n      }\n\n      inflight bam() {\n        this.b.put(\"hello\", \"world\");\n        this.b.get(\"aaa\");\n      }\n    }\n\t\t\"#)"
---
======================================================
Captured by MyType:
Variables:
  bam():
    this.b => [get(),put()]
  boom():
    this.b => [list()]
    this.s => []

======================================================
Files:

main.w
------------------------------------------------------

    bring cloud;
    
    class MyType {
      s: str;
      b: cloud.Bucket;

      init() { 
        this.s = "hello"; 
        this.b = new cloud.Bucket();
      }

      inflight boom() {
        assert(this.s.length > 0);
        assert(this.b.list().length > 0);
      }

      inflight bam() {
        this.b.put("hello", "world");
        this.b.get("aaa");
      }
    }
    
preflight.js
------------------------------------------------------
const cloud = require('@winglang/sdk').cloud;
class MyType extends $stdlib.std.Resource {
  constructor(scope, id, ) {
    super(scope, id);
    this._addInflightOps("boom", "bam", "$inflight_init");
    const __parent_this = this;
    this.s = "hello";
    this.b = this.node.root.newAbstract("@winglang/sdk.cloud.Bucket",this,"cloud.Bucket");
  }
  static _toInflightType(context) {
    return $stdlib.core.NodeJsCode.fromInline(`
      require("./inflight.MyType.js")({
      })
    `);
  }
  _toInflight() {
    return $stdlib.core.NodeJsCode.fromInline(`
      (await (async () => {
        const MyTypeClient = ${MyType._toInflightType(this).text};
        const client = new MyTypeClient({
          this_b: ${this._lift(this.b, ["get()", "list()", "put()"])},
          this_s: ${this._lift(this.s, [])},
        });
        if (client.$inflight_init) { await client.$inflight_init(); }
        return client;
      })())
    `);
  }
  _registerBind(host, ops) {
    if (ops.includes("$inflight_init")) {
      MyType._registerBindObject(this.b, host, ["get()", "list()", "put()"]);
    }
    if (ops.includes("bam")) {
      MyType._registerBindObject(this.b, host, ["get()", "put()"]);
    }
    if (ops.includes("boom")) {
      MyType._registerBindObject(this.b, host, ["list()"]);
      MyType._registerBindObject(this.s, host, []);
    }
    super._registerBind(host, ops);
  }
}

inflight.MyType.js
------------------------------------------------------
module.exports = function({  }) {
  class MyType {
    constructor({ b, s }) {
      this.b = b;
      this.s = s;
    }
    async boom()  {
      const __parent_this = this;
      {((cond) => {if (!cond) throw new Error(`assertion failed: '(this.s.length > 0)'`)})((this.s.length > 0))};
      {((cond) => {if (!cond) throw new Error(`assertion failed: '((await this.b.list()).length > 0)'`)})(((await this.b.list()).length > 0))};
    }
    async bam()  {
      const __parent_this = this;
      (await this.b.put("hello","world"));
      (await this.b.get("aaa"));
    }
    async $inflight_init()  {
      const __parent_this = this;
    }
  }
  return MyType;
}

