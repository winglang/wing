{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "compile": {
      "inputs": ["lib/**"],
      "outputs": [".jsii"]
    },
    "package": {
      "outputs": ["dist/**"]
    }
  }
}
