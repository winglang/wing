{
  "name": "@monadahq/wing-api-checker",
  "root": "apps/wing-api-checker/",
  "sourceRoot": "apps/wing-api-checker",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "inputs": ["src"],
      "outputs": ["dist"],
      "dependsOn": ["^build", "^build-wasm"]
    }
  }
}