{
  "name": "wing",
  "implicitDependencies": [
    "wingc",
    "wingsdk"
  ],
  "targets": {
    "copy": {
      "executor": "nx:run-commands",
      "inputs": [
        "rust-wasm"
      ],
      "dependsOn": [
        "^build-wasm"
      ],
      "outputs": [
        "wingc.wasm"
      ],
      "options": {
        "command": "cp ../../target/wasm32-wasi/debug/wingc.wasm ./",
        "cwd": "apps/wing"
      },
      "configurations": {
        "release": {
          "command": "cp ../../target/wasm32-wasi/release/wingc.wasm ./"
        }
      }
    },
    "build": {
      "executor": "nx:run-script",
      "dependsOn": [
        "^build",
        "copy"
      ],
      "options": {
        "cwd": "apps/wing",
        "script": "build"
      }
    }
  }
}
