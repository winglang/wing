import { memo } from "react";

export interface ScheduleInteractionViewProps {
  resourcePath: string;
}

export const ScheduleInteractionView = memo(
  ({ resourcePath }: ScheduleInteractionViewProps) => {
    return <></>;
  },
);
