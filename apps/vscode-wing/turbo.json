{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "compile": {
      "inputs": ["src/**"],
      "outputs": ["lib/**"]
    },
    "dev": {
      "dependsOn": ["compile"]
    },
    "package": {
      "outputs": ["vscode-wing.vsix"]
    }
  }
}
