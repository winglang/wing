{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "compile": {
      "dependsOn": ["^compile"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false
    },
    "eslint": {
      "outputs": []
    },
    "tsc": {
      "outputs": []
    },
    "vitest": {
      "outputs": ["coverage/**"]
    },
    "vitest:update": {
      "cache": false
    },
    "playwright": {
      "dependsOn": ["compile"],
      "outputs": ["playwright-report/**"]
    },
    "playwright:update": {
      "dependsOn": ["compile"],
      "cache": false
    }
  }
}
