{
  "name": "@wingconsole/monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "compile": "turbo run compile",
    "tsc": "turbo run tsc",
    "eslint": "turbo run eslint",
    "vitest": "turbo run vitest",
    "build": "turbo run tsc eslint vitest playwright compile",
    "test": "turbo run tsc eslint vitest playwright",
    "release": "conventional-release"
  },
  "devDependencies": {
    "@wingconsole/conventional-release": "workspace:^0.0.0",
    "turbo": "^1.8.3"
  }
}
