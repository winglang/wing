{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "compile": {
      "dependsOn": ["^compile"],
      "inputs": ["**/*.w", "**/*.js", "**/*.ts"],
      "outputs": ["target/testfixture.wsim/**"]
    },
    "topo": {}
  }
}
