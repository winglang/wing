export const CLIENTS_PACKAGE = "@monadahq/wingsdk-clients";
export const TERRAFORM_AWS_CLIENTS_PATH = require.resolve(
  `${CLIENTS_PACKAGE}/aws`
);
export const LOCAL_CLIENTS_PATH = require.resolve(`${CLIENTS_PACKAGE}/local`);
