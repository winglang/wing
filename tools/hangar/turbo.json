{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "test:generate": {
      "inputs": ["src/generate_tests.ts", "src/generated_test_targets.ts"],
      "outputs": ["src/test_corpus/**"],
      "cache": false
    },
    "test": {
      "dependsOn": ["^compile", "test:generate"],
      "inputs": ["src/**"],
      "cache": false
    },
    "bench": {
      "dependsOn": ["^compile"],
      "inputs": ["src/**"],
      "cache": false
    }
  }
}
