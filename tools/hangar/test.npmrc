# This config forces @winglang packages to pull through a local registry with dummy auth
# It is used by tests in this project
registry=http://localhost:4873
@winglang:registry=http://localhost:4873
//localhost:4873/:_authToken=hunter2
//localhost:4873/:_auth=hunter2
yes=true
audit=false
