{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "compile": {
      "dependsOn": ["^compile"],
      "inputs": ["**/*.w", "**/*.js", "**/*.ts"],
      "outputs": ["target/wing-fixture.wsim/**"]
    },
    "topo": {}
  }
}
