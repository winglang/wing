{
    "$schema": "https://turborepo.org/schema.json",
    "extends": ["//"],
    "pipeline": {
      "compile": {
        "dependsOn": ["^compile"],
        "inputs": ["**/*.w", "**/*.js", "**/*.ts"]
      },
      "topo": {}
    }
  }
